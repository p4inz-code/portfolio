# MASTER HANDOFF — atharvapatil.tech · 2026-08-09 · v5.0-beta.2

**Purpose:** single source of truth for the next chat session. This context is at ~98%; the next chat starts cold. Read this top-to-bottom before doing anything.

**Supersedes:** all prior handoffs (`MASTER-HANDOFF-2026-08-04.md` and everything before).

---

## 1. WHO THIS IS FOR

**Atharva Patil**

- Solo founder, **Northbyte Studios** (formerly Obsidian Labs / P4inZ — Obsidian Labs was 2025, Northbyte Studios started 2026). Based in Navi Mumbai, India.
- Contact: `atharva.patil.cg@gmail.com` · Discord `p4inz` · GitHub `p4inz-code` · no LinkedIn
- Session email: `masteratharva9@gmail.com`
- First-year VFX & 3D Animation student (D. Y. Patil University, B.Sc. Animation, 2025–2028 expected)
- Identity tagline: *"Developer by discipline. 3D artist by training. Builder by default."*
- Retired handles: "PainZ", "Grim". Real name now used publicly everywhere.
- Stack preferences: **Avalonia/.NET for desktop**, **vanilla JS for web** (Nexus is confirmed exception — **WPF**, not Avalonia. Mission OS uses **QML/Qt**).

---

## 2. STANDING WORKFLOW RULES (non-negotiable)

Applies to every task. Violating these has cost real time.

- **Ask before acting or building.** Plan and demo before touching real files.
- **"Plan mode"** = investigation only. **"GO"** = execute signal.
- **MAJOR.MINOR.PATCH** semver on every commit.
- Responses must be **concise** — "yap less, work more."
- **Brutal honesty over flattery.** Retract a suspected bug the moment verification disproves it.
- **Never fabricate project facts** — versions, dates, feature status. Verify against live sources (README, CHANGELOG, real URL). Flag contradictions, don't guess.
- **Validate every batch** — HTML parse, `node --check` on JS blocks, behavioral spot-check.
- **Changelog every version bump** with real reasoning. Footer / console / /status all sync.
- **Batch ~3 fixes per version** (working rhythm).
- Never oversell — status labels must reflect real state (Mission OS = "Active Development", never "production-ready").

## 3. SKILL ORCHESTRATION PROTOCOL (2026-08-09, standing)

Every task goes through **classify → select smallest strong skill combo → implement → verify with evidence**.

### Mandatory skill matrix

| Task type | Combo |
|---|---|
| Visual redesign | `taste-skill` + `minimalist-skill` + `frontend-design` + `ui-ux-pro-max` + `redesign-skill` + `verification-before-completion` |
| Typography | `taste-skill` + `frontend-design` + `minimalist-skill` + `ui-ux-pro-max` + `verification-before-completion` |
| Layout / spacing | `taste-skill` + `minimalist-skill` + `ui-ux-pro-max` + `frontend-design` + `verification-before-completion`. Order: alignment → spacing → hierarchy → components → decoration. |
| Component | `taste-skill` + `ui-ux-pro-max` + `frontend-design` + `redesign-skill` + `receiving-code-review` + `verification-before-completion`. Reuse first. |
| A11y | `ui-ux-pro-max` + `webapp-testing` + `verification-before-completion`. Run `npx @axe-core/cli <URL>` when appropriate. |
| Perf | `ui-ux-pro-max` + `webapp-testing` + `verification-before-completion`. Run `npx lighthouse@latest <URL>` and compare vs baseline. No claim without measurement. |
| Bug fix | `systematic-debugging` + domain skill + `receiving-code-review` + `verification-before-completion`. Reproduce → root cause → smallest fix → reproduce again. |
| Refactor | `writing-plans` + `receiving-code-review` + `simplify` + `verification-before-completion`. |
| New feature | `writing-plans` + design + engineering + `test-driven-development` + `verification-before-completion`. |
| Planning large change | `writing-plans` + `using-superpowers`. Plan must contain current state / problem / desired outcome / files affected / smallest implementation / verification strategy / rollback. |
| Git / release | `verification-before-completion` + `requesting-code-review`. `git status → git diff --stat → git diff` before every commit. |
| Deployment | `verification-before-completion` + testing + perf. After deploy: verify HTTP / assets / routes / rendering / Lighthouse. Never assume Cloudflare deployed because `git push` succeeded. |

### Design task special rule
Default operation is **REMOVE → REFINE → ALIGN → TYPOGRAPHY → ADD**. Never `ADD → ADD → ADD → ANIMATE`.

### Skill conflict priority
`Production safety → Existing architecture → Existing brand identity → Accessibility → Usability → Performance → Taste → Novelty`. Novelty always loses.

### Response format at completion (mandatory)
```
TASK:
SKILLS USED:
CHANGED:
PRESERVED:
TESTED:
REVIEWED:
REGRESSIONS:
STATUS:
```
Keep concise. Never claim success from "code looks right" / "command ran". Evidence required.

---

## 4. SKILLS INSTALLED (34 total, in `.claude/skills/`)

### From `nextlevelbuilder/ui-ux-pro-max-skill` (2026-08-05)
`banner-design`, `brand`, `design`, `design-system`, `slides`, `ui-styling`, `ui-ux-pro-max`

### From `Leonxlnx/taste-skill` (2026-08-05)
`brandkit`, `brutalist-skill`, `gpt-tasteskill`, `image-to-code-skill`, `imagegen-frontend-mobile`, `imagegen-frontend-web`, `minimalist-skill`, `output-skill`, `redesign-skill`, `soft-skill`, `stitch-skill`, `taste-skill`, `taste-skill-v1`

### From `anthropics/skills` (2026-08-05)
`algorithmic-art`, `brand-guidelines`, `canvas-design`, `frontend-design`, `theme-factory`, `web-artifacts-builder`, `webapp-testing` (added 2026-08-09)

### From `obra/superpowers` (2026-08-09)
`using-superpowers`, `verification-before-completion`, `receiving-code-review`, `requesting-code-review`, `writing-plans`, `systematic-debugging`, `test-driven-development`

### On the machine
- Node 24.14.0 + npm 11.9.0 · Python 3.11.9 + 3.14.3 · Rust 1.97.1 + cargo
- `npx lighthouse@latest` proven working (v3.35.3 audit)
- `npx @axe-core/cli <URL>` on demand
- Chrome at `C:/Program Files/Google/Chrome/Application/chrome.exe`

### Explicitly SKIPPED with reason
- `anthropics/claude-code` (meta) · `chopratejas/headroom` (Rust token-compression, not a skill) · `OWASP/CheatSheetSeries` (WebFetch on demand) · `semgrep/semgrep` (zero attack surface here) · `dequelabs/axe-core` (npx on demand) · `GoogleChrome/lighthouse` (npx on demand) · `cli/cli / gh` (solo repo, direct-push workflow, no PRs).

---

## 5. PROJECT ARCHITECTURE — TWO PARALLEL STATES

### `main` branch (live at atharvapatil.tech) — **v4.7**
- Single self-contained `index.html`, ~15.9 MB with inline base64 images
- Vanilla HTML + inline CSS + inline JS + in-memory JS router
- Cloudflare Pages auto-deploys on `git push origin main`
- Files at root: `index.html`, `_headers`, `robots.txt`, `security.txt`, `llms.txt`, `README.md`, `LICENSE`, `CLAUDE.md`
- Palette v4.0 (Ultraviolet Cathedral) + typography (PP Editorial New) already live
- Command palette (⌘K / press `/`) live
- 20 changelog entries in the /status route

### `v5-astro` branch — **v5.0-beta.2**
- Full Astro migration in **`portfolio-v5/`** subdirectory
- Astro 5.18.2, static output, zero-JS-by-default, Cloudflare Pages native (no adapter)
- Isolated from live site — nothing here reaches production yet
- 13/13 pages migrated, 100% content parity
- Component library + data layer + dual-theme + real Kanvaz screenshots
- Build: `npm run build` → `dist/`, ~700 ms, 13 HTML files
- **DO NOT MERGE TO MAIN** until Cloudflare Pages build config is switched (see §12)

Current git state (from latest `git log`):
```
062919f  v5.0-beta.2: reusable Screenshot component + Kanvaz layout proper
7b80472  v5.0-beta: data layer + component library + all 13 pages migrated
38d8416  v5.0-alpha: Astro bootstrap in /portfolio-v5/ + 8 workflow skills
df9396d  v4.7: info-page headline pass (main branch tip)
```

---

## 6. FILE STRUCTURE

### Root of repo (`C:\Users\Admin\Desktop\portfolio-zip\portfolio\`)
```
├── index.html            (v4.7 live site — 15.9MB monolith)
├── _headers              (Cloudflare Pages CSP + security headers)
├── robots.txt · security.txt · llms.txt
├── CLAUDE.md             (project instructions, standing rules)
├── README.md · LICENSE
├── .gitignore
├── docs/                 (spec files)
│   ├── pursue-os-spec.md
│   └── HANDOFF-2026-08-09-v5.md  ← YOU ARE HERE
├── assets/inbox/         (empty — dropped assets go here)
├── memory/               (project-scoped memory records, mirror of user memory)
├── .claude/skills/       (34 installed skills)
└── portfolio-v5/         (v5 Astro workspace)
```

### `portfolio-v5/` (Astro workspace)
```
├── package.json            (astro ^5.0.0, npm scripts)
├── astro.config.mjs        (static output, inlineStylesheets: auto, compressHTML)
├── tsconfig.json           (extends astro/tsconfigs/strict)
├── .gitignore              (node_modules, dist, .astro)
├── node_modules/           (149 packages, git-ignored)
├── dist/                   (build output, git-ignored)
├── public/
│   ├── _headers · robots.txt · security.txt · llms.txt   (copied from live)
│   ├── favicon.svg         (minimal AP-style glyph)
│   └── assets/
│       ├── brand/
│       │   └── ap-monogram.png   (14.6 MB — TOO BIG, unwired, needs resize)
│       └── kanvaz/
│           ├── kanvaz-01-start.png
│           ├── kanvaz-02-board.png                 (1 MB, board view — hero)
│           ├── kanvaz-03-settings-appearance.png
│           ├── kanvaz-04-settings-developer.png
│           ├── kanvaz-05-about.png                 (v4.2.1 build shown)
│           └── kanvaz-06-shortcuts.png
└── src/
    ├── data/
    │   ├── projects.ts     (SINGLE SOURCE OF TRUTH for all 9 projects)
    │   └── site.ts         (SITE, CONTACT, NAV, SERVICES constants)
    ├── styles/
    │   ├── tokens.css      (dual-theme design tokens)
    │   └── global.css      (reset, font imports, a11y utilities)
    ├── components/
    │   ├── ThemeToggle.astro
    │   ├── ui/
    │   │   ├── StatusPill.astro
    │   │   ├── Tag.astro
    │   │   ├── Button.astro
    │   │   └── MetaTag.astro
    │   ├── layout/
    │   │   ├── Section.astro
    │   │   └── CaseHero.astro
    │   └── features/
    │       ├── EditorialHero.astro
    │       ├── ProjectCard.astro
    │       ├── Screenshot.astro
    │       └── CommandPalette.astro
    ├── layouts/
    │   └── BaseLayout.astro (sticky nav + no-flash theme boot + footer + cmd+K trigger)
    └── pages/
        ├── index.astro         (home — 3-act hero + bento)
        ├── work.astro
        ├── services.astro
        ├── about.astro
        ├── resume.astro
        ├── contact.astro
        ├── 3d.astro
        ├── mission-os.astro
        ├── nexus.astro
        ├── kanvaz.astro        (now with 6 real screenshots in proper Screenshot frames)
        ├── pursue-os.astro     (NEW — from user's spec)
        ├── status.astro
        └── accessibility.astro
```

**Key architectural rule:** pages read from `data/projects.ts` and `data/site.ts` — **never hardcode** project metadata in a page.

---

## 7. DESIGN SYSTEM

### Palette — "Ultraviolet Cathedral" (dark, default)
```
--bg:         #0A0517   (deep midnight-purple base)
--surface-1:  #150B25
--surface-2:  #1F1338
--surface-3:  #2B1B4F
--line:       rgba(232, 225, 208, 0.10)
--line-strong:rgba(184, 157, 255, 0.28)
--text:       #E8E1D0   (parchment cream — INVERTS the usual dark-portfolio pattern)
--muted:      #9B95B0
--muted-2:    #726D88
--purple:     #B993FF
--purple-bright:#D6BFFF
--purple-deep: #7B3FE4
--green:      #7CE0A8
--scrim:      rgba(10,5,23,0.72)
```

### Palette — "Parchment Manuscript" (light, added in v5.0)
```
--bg:         #F3EDDD   (parchment cream, now BG)
--surface-1:  #EAE2CE
--surface-2:  #DFD5BC
--surface-3:  #CCC0A2
--text:       #1A0F28
--muted:      #4E4160
--muted-2:    #6E6383
--purple:     #6E44D6   (darker so it holds contrast on cream)
```
Toggle: localStorage `nb-theme` (`dark` | `light`). Initial visit respects `prefers-color-scheme`. Keyboard shortcut: `T` anywhere.

### Typography — "Sacred Text" stack
- Display + body: **PP Editorial New** (Fontshare, free)
- Mono (code, kbd, eyebrows): **JetBrains Mono** (Google Fonts, free)
- Emphasis pattern: `<em>italic + purple-bright</em>` on key phrases
- Fraunces / Inter / Space Grotesk — all removed in v4.0
- Fluid scale via `clamp()` — see `src/styles/tokens.css`

### Motion
- Curve: `--ease-out: cubic-bezier(0.16, 0.8, 0.24, 1)`
- Durations: 150 / 300 / 500 ms
- `prefers-reduced-motion: reduce` overrides all animation-duration + transition-duration to 0.001ms globally

### Signature moves already integrated
1. **Three-act narrative hero** on home (borrowed from emilianmisera.com — chronological personal story, not skill-showcase)
2. **⌘K / `/` command palette** (universal shortcut)
3. **Bento variety** on home (featured project full-width, others 6-col halves) — breaks "all cards look same"

### Signature moves DEFERRED (user picked 4 from reference sites)
- **Emilian Misera:** done ✓ (3-act hero above)
- **Gabriel Beaugonin:** "unexpected scroll animation — no heavy yet clean and surprising" — NOT YET IMPLEMENTED
- **Good Growth:** interactive skeuomorphic hero object (their disc metaphor) — translate as install-disk / vault-unlock — NOT YET
- **Pouya Shahri:** entire portfolio as mock OS — user wants a **dedicated interactive Mission OS demo page** — NOT YET

---

## 8. PROJECT ROSTER — REAL CURRENT STATE

**Master source of truth:** `portfolio-v5/src/data/projects.ts`. Every field is typed. Verify against this file before making claims about any project.

| Project | Status | Current | Notes |
|---|---|---|---|
| **Mission OS** | Active Development | Pre-release, ISO in internal testing | GPLv3, Debian Stable + KDE Plasma, QML/Qt installer. First ISO for public testing targeting mid-August 2026. Repo: `p4inz-code/mission-os`. |
| **Nexus** | Public Beta | v10.12.0 | Windows encrypted vault. C# / .NET 8 / **WPF** (NOT Avalonia — corrected in v3.33). AES-256-GCM (NXG1 format shipped v10.12.0 — before that was actually AES-256-CBC despite docs claiming GCM), Argon2id KDF, Windows Hello unlock, in-app SHA-256-verified updater. Source proprietary. Repo `p4inz-code/nexus-desktop` hosts releases + Discord community only. Discord invite: `https://discord.gg/XFF5nV53ZJ`. |
| **Kanvaz** | Final Release | v4.2.2 | VFX reference workspace. Electron + vanilla JS. MIT. **User confirmed v4.2.2 is shipped**; screenshot in case study shows v4.2.1 build (older) — case study caption states this honestly. v4.3.0 stabilization is the last major work, then maintenance-only. Repo: `p4inz-code/kanvaz`. |
| **Pursue OS** | Coming Soon | Pre-release | NEW project (added v5.0-beta). Investigation-focused Linux OS for OSINT / DFIR. Apache 2.0. Two flagship interfaces: Investigation Terminal + Investigation Browser (Tor). AI is assistant-not-authority. Full spec in `docs/pursue-os-spec.md`. Repo: `p4inz-code/pursue-os`. Launch ~1 month out. |
| **Veris** | Final Polish · Pre-v1.0.0 | Pre-release | Windows security investigation platform. TypeScript / Node.js / SQLite. Deterministic engine — no black-box AI scoring. 3,100+ tests. Case study still pending assets. User will provide update 2026-08-10 (24hr reminder). Repo: `p4inz-code/veris`. |
| **repo-map** | On Hold | v2.2.0 | CLI + planned TUI. TypeScript, npm `@p4inz-code/repo-map`. MIT. Interactive TUI paused. Repo: `p4inz-code/repo-map`. |
| **Obscura** | Released | v0.9.0 | Luau AST toolkit for Roblox. MIT. Active side project. Repo: `p4inz-code/obscura`. |
| **Glint** | **On Hold** | Internal testing complete | Windows tray brightness/volume utility. C# / .NET 8 / Avalonia. Repo: `p4inz-code/glint`. Updated to "On Hold" in 5 places in v4.5. |
| **3D Ref Skills** | Live / Maintained | Stable | Open-source knowledge graph. Repo: `p4inz-code/3d-ref-skills`. |

### Featured on homepage (in order)
1. Mission OS
2. Nexus
3. Kanvaz
4. Pursue OS

### Supporting (secondary grid)
Veris, repo-map, Obscura, Glint, 3D Ref Skills

---

## 9. REFERENCES USER PICKED (for design inspiration)

User's 4 reference sites and what to borrow from each:

| # | Site | Move to borrow |
|---|---|---|
| 1 | https://www.emilianmisera.com/ | Three-act narrative hero (personal chronological story). **DONE.** |
| 2 | https://www.gabrielbeaugonin.com/ | User's own words: *"unexpected scroll animation — no heavy yet clean and surprising"* (SKIP his heavy scroll, want the surprise quality). **TODO.** |
| 3 | https://goodgrowth.com/ | Interactive skeuomorphic hero object (their insert-disc metaphor) — translate to install-disk / vault-unlock. **TODO.** |
| 4 | https://pouyashahri.com/ | Portfolio IS an OS. Direct fit — Atharva builds OSes. Wants a **dedicated interactive Mission OS demo page** that feels like a boot-into-desktop. **TODO.** |

---

## 10. DEFERRED / BLOCKED / STILL NEEDED

### Deferred (my responsibility to execute when unblocked)

- **v5.1 — signature moves 2/3/4** (Gabriel scroll, Good-Growth interactive object, Pouya OS-metaphor threading)
- **Interactive Mission OS demo page** (Shahri-style, user explicitly asked for this)
- **AP monogram** in nav — file present at `public/assets/brand/ap-monogram.png` but **14.6 MB unresized**. Not wired. Layout still uses inline SVG placeholder. Waiting on user to send a resized version (200×200 PNG or ideally SVG).
- **Cloudflare Pages build config swap** — needed to flip live URL from v4.7 monolith → v5 Astro. Requires dashboard change:
  - Build command: `cd portfolio-v5 && npm install && npm run build`
  - Output directory: `portfolio-v5/dist`
  - **Do not do this without explicit user OK** — it changes what visitors see.
- **Full historical changelog port** — v5 `/status` currently only has the v5.0-alpha entry; v4.7 → v3.16 history lives on the live site's `/status` and needs to be brought forward as data.
- **Nexus + Mission OS screenshot integration** using the new `Screenshot.astro` component (same pattern as Kanvaz) — will apply as real assets arrive.

### Blocked on user assets

- **Veris production screenshots** — user promised 2026-08-10 (**24hr reminder pending**)
- **VFX renders / turntables** — user promised 2026-08-10 or 2026-08-11
- **Mission OS installer screenshots** (Security Options especially) — arrives when ISO ships (targeting mid-August)
- **Resized AP monogram** — 200×200 PNG or SVG
- **Fresh Kanvaz About screenshot at v4.2.2** — user said not urgent, can update when v4.3.0 ships and stabilizes everything

### Content honesty flags currently on the site

- Kanvaz About caption on `/kanvaz` states *"v4.2.1 build shown (v4.2.2 is current)"* — honest, user OK'd this
- Mission OS Security Options is a **labeled concept placeholder** — user said this is intentional until ISO ships
- Nexus "no public repository" note replaced in v4.5 with *"Source is proprietary; the public repo hosts releases and community discussion only"* plus Releases + Discord links
- Glint "On Hold" everywhere as of v4.5

---

## 11. VERSION HISTORY (this repo, this year)

Compressed changelog — see `/status` on live site for full detail.

| Version | Summary |
|---|---|
| v3.16 | Console error resolved, og:image prepped |
| v3.18 | CLS regression + mobile ring alignment fixed |
| v3.21 | Kanvaz content updated to real v3.6.9 release data |
| v3.32 | Last stable pre-content-refresh build |
| v3.33 | Nexus content deep-dive (WPF corrected from Avalonia; Public Beta) |
| v3.34 | Hybrid router (real Back/Forward + deep-linking); ring pathLength fix; iOS input auto-zoom fix |
| **v3.35** | Content sync — Mission OS added first-featured; Nexus v10.12.0 + AES-GCM everywhere; Kanvaz v4.2.2 final; Veris polish-phase; misleading Nexus GitHub buttons → Discord; Glint copy fix; llms.txt rewrite |
| v3.35.1 | Homepage/Work project-row layout fix (correct 3-child grid) |
| v3.35.2 | Alt-text v3.7.1 → neutral; skills install; CLAUDE.md wired |
| v3.35.3 | Lighthouse: 5 WCAG-AA contrast fails fixed; CSP allowed Cloudflare Insights beacon; stale v3.34 tags bumped |
| **v4.0** | Ultraviolet Cathedral palette + PP Editorial New typography; em-dash purge (232); emoji feature-icons → SVG; numbered section-labels retired |
| v4.1 | Home polish: thumbnail crop fix, meaningful service+credential icons, simplified AP mark with hover-sheen |
| v4.2 | Simplification pass — pillar-syms/custom cursor/3D perspective reveal/card tilt removed; orbs toned |
| v4.3 | Card tilt restored (user asked); AP mark got a single orbit dot |
| v4.4 | Resume page redesign — editorial hero, meta grid, downloads pinned to header, killed 8× repeating eyebrow pattern |
| v4.5 | ⌘K command palette (the "something new beyond HTML" ask); Glint "On Hold" in 5 places; Nexus Releases + Discord links on resume |
| v4.6 | Case-study editorial polish (Nexus, Kanvaz, Mission OS) — italic titles, cap-compliant eyebrows |
| v4.7 | Info-page headline pass — editorial h1, italic-purple emphasis, consistent across all 6 pages |
| **v5.0-alpha** | Astro bootstrap in `/portfolio-v5/` + 8 superpowers skills + Pursue OS spec doc |
| **v5.0-beta** | Data layer + component library + all 13 pages migrated |
| **v5.0-beta.2** | Reusable `Screenshot` component + Kanvaz layout proper (6 real screenshots wired) |

---

## 12. DEPLOYMENT — HOW CLOUDFLARE PAGES CURRENTLY WORKS

- Repo: `github.com/p4inz-code/portfolio` (all-rights-reserved, not open source)
- Cloudflare Pages project connected to `main` branch
- **Current build settings:** no build command, serve from repo root, root file is `index.html`
- A `git push origin main` = live deploy in ~30s

### To swap to v5 Astro (when ready)
1. Cloudflare Pages → project settings → Build & Deployments
2. Build command: `cd portfolio-v5 && npm install && npm run build`
3. Output directory: `portfolio-v5/dist`
4. Root directory: leave blank (repo root)
5. Node version: 20 or 24
6. Then merge `v5-astro` → `main`, push
7. Verify: check every route, run Lighthouse, curl `_headers`

**Rollback plan:** revert Cloudflare build settings to original (no build, serve root, `index.html`). Old monolith at repo root still works.

---

## 13. HOW TO RUN THINGS

```bash
# from repo root
cd portfolio-v5

# dev server — http://localhost:4321
npm run dev

# production build — outputs to portfolio-v5/dist
npm run build

# preview production build
npm run preview
```

**Lighthouse on live:**
```bash
npx lighthouse@latest https://atharvapatil.tech/ --preset=perf --form-factor=mobile --chrome-flags="--headless=new --no-sandbox"
```

**Axe accessibility check on live:**
```bash
npx @axe-core/cli https://atharvapatil.tech/
```

**Git — always on `v5-astro` for v5 work, NEVER on main:**
```bash
git checkout v5-astro
git pull origin v5-astro
# ... make changes ...
git status && git diff --stat && git diff
git commit -m "..."
git push origin v5-astro
```

---

## 14. LIGHTHOUSE BASELINE (2026-08-05 on v3.35.3)

Mobile + desktop scores at that build:
- **Performance:** 55 (both)
- **Accessibility:** 96 → **100** after v3.35.3 patches
- **Best Practices:** n/a (CSP error fixed → will score)
- **SEO:** 100

**#1 perf blocker:** 11.2 MB HTML dominates FCP/LCP. Astro migration should collapse this by ~90% once base64 images are externalized. **Re-run Lighthouse after v5 goes live** to prove the gain.

---

## 15. LIVE URLS TO KNOW

- Portfolio: https://atharvapatil.tech
- GitHub profile: https://github.com/p4inz-code
- Nexus repo: https://github.com/p4inz-code/nexus-desktop (releases + Discord only)
- Nexus Discord: https://discord.gg/XFF5nV53ZJ
- Kanvaz repo: https://github.com/p4inz-code/kanvaz
- Mission OS repo: https://github.com/p4inz-code/mission-os
- Pursue OS repo: https://github.com/p4inz-code/pursue-os
- Veris repo: https://github.com/p4inz-code/veris
- repo-map: https://github.com/p4inz-code/repo-map + npm `@p4inz-code/repo-map`
- Obscura repo: https://github.com/p4inz-code/obscura
- Glint repo: https://github.com/p4inz-code/glint
- 3D Ref Skills repo: https://github.com/p4inz-code/3d-ref-skills

### Freelance client
- Kalasadhana Academy of Performing Arts — https://kalasadhana-navimumbai.in — music school in Kharghar. Weekly Sunday maintenance window via Hostinger hPanel File Manager. Coding rules for THAT project only: var-only JS, no arrow functions, no `.forEach()`, all `querySelector`/`getElementById` calls null-guarded, one combined scroll listener, relative asset paths.

---

## 16. USER MEMORY (in `~/.claude/projects/…/memory/`)

Five records already saved:
- `feedback_skill_orchestration_protocol.md` — 2026-08-09 standing protocol
- `feedback_mandatory_design_skills.md` — always load ui-ux-pro-max + taste-skill + redesign-skill
- `feedback_workflow_rules.md` — ask before acting, plan+demo, brutal honesty, semver
- `feedback_project_row_grid.md` — `.project-row` needs exactly 3 direct children
- `user_atharva.md` — user identity
- `project_portfolio_site.md` — site facts

New chat should read these at session start; they auto-load into context.

---

## 17. STANDING REMINDERS

- **Veris check-in scheduled for 2026-08-10** — user promised update, surface it when they come back
- **VFX renders arriving 2026-08-10 or 2026-08-11**
- **Kanvaz v4.3.0 shipping soon** — when it does, refresh About screenshot + stabilize all site references
- **AP monogram resize** — needed before it can go into the nav brand-mark
- **Cloudflare build-config swap** — needs explicit user OK; do not do proactively

---

## 18. THE FIRST 5 MOVES FOR THE NEXT SESSION

1. **Read this doc top to bottom.** Then read `CLAUDE.md`, then `memory/*.md`.
2. **Check out `v5-astro` branch** (`git checkout v5-astro`), never touch main.
3. **Run `cd portfolio-v5 && npm install && npm run build`** to confirm the workspace still builds clean.
4. **Ask user which iteration path to take** — v5.1 signature moves? Interactive OS demo? Cloudflare swap? Real assets integration?
5. **Report per the fixed response format** at every commit.

**Do not:**
- Commit to `main`
- Modify the live `index.html`
- Try to install / merge / push without user's explicit GO
- Bulk-add dependencies to `portfolio-v5/package.json`

---

*End of handoff. Written 2026-08-09 at v5.0-beta.2. Superseded only when a new HANDOFF is written and this one is retired.*
